# SandBase

> AI agent infrastructure platform. One account to access 2,000+ AI models and APIs for LLM, image, video, audio, search, data, and agent workflows.

## API Base URL

https://api.sandbase.ai/v1

## Authentication

Bearer token: `Authorization: Bearer sk-YOUR_KEY`

## Connect AI Tools

Guided Setup: https://www.sandbase.ai/console/setup

Choose a client in Setup and follow the exact guidance shown there. For a CLI-supported client such as Codex:

`curl -fsSL https://sandbase.ai/install.sh | sh -s -- --client codex`

Read-only installer client catalog:

`curl -fsSL https://sandbase.ai/install.sh | sh -s -- --list-agents`

Diagnose a CLI-managed client:

`npx -y @sandbaseai/cli doctor --client codex`

Installation and authorization are not proof of connectivity. Verify the SandBase entry and tools in the target client, then complete one safe tool request.

Open-source CLI: https://github.com/sandbaseai/cli

## Core Endpoints

- POST /v1/chat/completions - OpenAI-compatible chat (LLM, vision, tools, streaming)
- POST /v1/messages - Anthropic-compatible messages API
- POST /v1/embeddings - Generate text embeddings
- POST /v1/run - Unified generation (image, video, audio, any async task)
- GET /v1/run/{id} - Poll async generation status
- GET /v1/models - List enabled logical models (OpenAI-compatible; defaults to type=llm)
- GET /v1/models/{id_or_name} - Get model details, schema, capabilities, and pricing
- GET /v1/tasks/{id}/cost - Get task cost and usage

## Agent Lifecycle

- POST /v1/agents - Create an agent
- POST /v1/agents/{id} - Update an agent and create a version
- POST /v1/agents/{id}/archive - Archive an agent
- POST /v1/endpoints/{id}/run - Invoke a Service (Endpoint) and create or continue a Session
- POST /v1/sessions - Create agent session directly
- POST /v1/sessions/{id}/events - Send messages to running session
- GET /v1/sessions/{id}/events/stream - Replay persisted session events (SSE)
- POST /v1/deployments - Create a Schedule (Deployment)
- POST /v1/deployments/{id}/runs - Create a manual DeploymentRun
- GET /v1/deployment_runs - List DeploymentRuns across Schedules

## Session Contract

- session_id is the only public identity for persistent Agent interaction.
- Endpoint invocation accepts an optional session_id. Omit it to create a Session; provide it to continue that Session.
- Every manual or scheduled Deployment trigger creates a distinct drun_* DeploymentRun and attempts to create exactly one new Session.
- A DeploymentRun links to session_id when Session creation succeeds; failed trigger records have no Session.
- Endpoint acceptance returns session_id and an events array. It does not create a DeploymentRun or expose an internal Runtime Session identity.

## Pricing

Pay-per-use. No monthly minimums.
Pricing formulas differ by model. Use GET /v1/models for discovery and GET /v1/models/{id_or_name} for the current
model card, capabilities, input schema, and detailed pricing. Use GET /v1/tasks/{id}/cost for recorded task cost.

## Docs

- Connect AI tools: https://www.sandbase.ai/docs/setup/
- CLI source: https://github.com/sandbaseai/cli
- AI API guide: https://www.sandbase.ai/docs/for-agents/full
- Models & Pricing: https://www.sandbase.ai/docs/for-agents/models
- Error guide: https://www.sandbase.ai/docs/for-agents/errors
- OpenAPI spec: https://www.sandbase.ai/docs/openapi.yaml
- Expanded LLM guide: https://www.sandbase.ai/docs/llms-full.txt
